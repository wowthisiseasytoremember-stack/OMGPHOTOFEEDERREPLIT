## Packages
framer-motion | Page transitions and detailed animations
clsx | Conditional class names utility
tailwind-merge | Utility for merging Tailwind classes

## Notes
Browser Pilot requires Chrome running with --remote-debugging-port=9222
Image uploads use the /api/analyze/upload endpoint
Mobile detection is handled via window.matchMedia
